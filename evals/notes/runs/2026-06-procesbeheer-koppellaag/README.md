# Run snapshot — procesbeheer + koppellaag (full 7-bolt program), 2026-06

Second doc-slimming run (TP-6 full-intent generalization, after BTW units 002/003).
Method/why: ../../doc-slimming-eval-framework.md · how-to: ../../doc-slimming-replay-playbook.md

- stage-a.md — doc economy + caveat survival + hard gate (cheap funnel)
- stage-b.md — 10/10 green builds + blind 3-judge panel + Pareto verdict  ← the verdict
- RUN.md — the run log (scope, frozen bases, build matrix, phase status)

Live workspace (arms, anonymized judge diffs, build-recipe, caveats, ac-checklist):
  ../../doc-slimming/procesbeheer-koppellaag/

## One-line verdict
Generalizes. aidlc-turbo (−73% doc lines) Pareto-DOMINATES the heavy AI-DLC control on BOTH economy
and build quality (8.06 vs 7.67); the lean inferno control is worst (6.96) and gate-fails on the
caveats it dropped (wrong soort column, wrong list-filter column, unwired delete guard). Frontier =
{aidlc-turbo = best quality/balance, turbo-a = best economy −83%}. turbo-a did NOT repeat as BTW's
#1 (kl double-encoding slip) → aidlc-turbo is the robust cross-intent default. Next: financial-
middleware cross-app check (../../doc-slimming/todo.md).
