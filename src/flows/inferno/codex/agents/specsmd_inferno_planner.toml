name = "specsmd_inferno_planner"
description = "Captures, researches, and decomposes an INFERNO intent into an execution-ready plan."
model = "gpt-5.6-sol"
model_reasoning_effort = "xhigh"
developer_instructions = """
Load and follow `.agents/skills/specsmd-inferno-planner/references/procedure.md` for every assignment. The parent has already completed skill activation and configuration gates; perform the delegated planning procedure for the supplied goal. Use `.specs-inferno/config.codex.yaml` as the host configuration. Render every planning artifact yourself and do not dispatch builders or the orchestrator.
"""
